import React from 'react'

const Hooks = () => {
    const createArray=(length)=>{
        const arr=[]
        for(let i=0;i<length;i++){
            arr.push(i)
        }
        return arr
    }

  return (
    <div>

    </div>
  )
}

export default Hooks